// ---------------------------------------------------------
// レイヤー定義.
// ---------------------------------------------------------
public class LayerConst {

    // Canvas用のレイヤー.
    // Tags and LayersのSortingLayersLayersの内容と一致させる.
    // 通常時はMiddleを使う.
    public enum CanvasLayer {
        Background = 0,
        UI = 1,
        Front = 2,
    }
}
